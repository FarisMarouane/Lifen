import React from 'react';

const ServerInfo = ({ numberOfBinary }) => {
  return (
    <div>
      <h4>Total Binary on the server: {numberOfBinary}</h4>
    </div>
  );
};

export default ServerInfo;
